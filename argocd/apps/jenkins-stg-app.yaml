apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jenkins-stg
  namespace: argocd
spec:
  project: platform

  sources:
    - repoURL: https://charts.jenkins.io
      chart: jenkins
      targetRevision: 5.8.6
      helm:
        releaseName: jenkins-stg
        valueFiles:
          - $values/argocd/helm-values/jenkins/stg-values.yaml

    - repoURL: https://github.com/mimyrtek/platform-gitops.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: jenkins-stg

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
