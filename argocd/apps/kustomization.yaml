apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - jenkins-dev-rbac.yaml
  - jenkins-stg-rbac.yaml
  - jenkins-prd-rbac.yaml
  - platform-project.yaml
  - jenkins-dev-app.yaml
  - jenkins-stg-app.yaml
  - jenkins-prd-app.yaml
  - minio-tfstate-app.yaml
  - nexus-app.yaml
  - registry-ui-app.yaml
  # nexus-iq disabled by default (~3-4Gi RAM + needs a license) — uncomment to enable
  # - nexus-iq-app.yaml
