apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# The nexus namespace is created by the Application's CreateNamespace=true
# syncOption, so it is not shipped as a manifest here.
namespace: nexus
resources:
  - deployment.yaml
  - service.yaml
